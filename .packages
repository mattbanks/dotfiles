#!/bin/bash

# RVM and Gems
rvm install 1.9.3
rvm use 1.9.3 --default
gem update --system
gem install rails
gem install compass

# Homebrew
brew update
brew upgrade
brew tap homebrew/dupes
brew install ack
brew install git
brew install node
brew install z

# NPM Packages
npm install -g jshint
npm install -g grunt
